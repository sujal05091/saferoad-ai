<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <!-- Camera Container (Hidden until ride starts) -->
    <FrameLayout
        android:id="@+id/cameraContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <!-- Rear Camera Preview (Full Screen or 3/4 screen in dual mode) -->
        <androidx.camera.view.PreviewView
            android:id="@+id/rearPreviewView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Front Camera Preview (1/4 screen from top in dual mode) -->
        <androidx.camera.view.PreviewView
            android:id="@+id/frontPreviewView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_gravity="top"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Initial Screen with Animation -->
    <LinearLayout
        android:id="@+id/initialScreen"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- Detection Animation (Large, Centered) -->
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/detectingAnimation"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:padding="40dp"
            app:lottie_rawRes="@raw/anim_detecting"
            app:lottie_loop="true"
            app:lottie_autoPlay="true" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ready to Start"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/colorPrimary"
            android:layout_marginTop="24dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI-powered road safety monitoring"
            android:textSize="16sp"
            android:textColor="#666666"
            android:gravity="center"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="32dp" />
    </LinearLayout>

    <!-- Overlay Detection Indicator (Shows when detecting during ride) -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/overlayDetectingAnimation"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="24dp"
        app:lottie_rawRes="@raw/anim_detecting"
        app:lottie_loop="true"
        app:lottie_autoPlay="false"
        android:visibility="gone"
        android:elevation="8dp" />

    <!-- Detection Display Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/detectionCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="80dp"
        android:layout_marginHorizontal="16dp"
        android:visibility="gone"
        android:elevation="12dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="#F5F5F5">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/detectionTypeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⚠️ Pothole Detected"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#D32F2F" />

            <TextView
                android:id="@+id/detectionDetailsText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Confidence: 85%"
                android:textSize="14sp"
                android:textColor="#666666"
                android:layout_marginTop="4dp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Start/Stop Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/startStopButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="48dp"
        android:paddingHorizontal="64dp"
        android:paddingVertical="18dp"
        android:text="START RIDE"
        android:textSize="18sp"
        android:textStyle="bold"
        app:cornerRadius="30dp"
        app:icon="@android:drawable/ic_media_play"
        app:iconGravity="start"
        android:elevation="8dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
